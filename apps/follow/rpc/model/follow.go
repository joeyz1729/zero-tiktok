package model

//func RelationAction(userId, toUserId int64) (cancel bool, err error) {
//	followRes := new(Follow)
//	sqlStr := fmt.Sprintf(`select %s from tiktok_follow.follow where user_id = ? and follower_id = ? limit 1`, followRows)
//	err := l.svcCtx.FollowDB.Select(followRes, sqlStr, in.UserId, in.ToUserId)
//
//	return
//}
