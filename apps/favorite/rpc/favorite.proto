syntax = "proto3";

package favorite;
option go_package="./favorite";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Favorite {
  rpc Ping(Request) returns(Response);
}
